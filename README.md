# TinyDOM
A tiny DOM implementation for use in C at low-resource, legacy and embedded systems
Pretty simple pointer-driven API, the structs can and are recommended to be used raw, sort of like cJSON and other lightweight libs
